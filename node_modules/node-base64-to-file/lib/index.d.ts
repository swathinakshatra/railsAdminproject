declare function base64toFile(base64: string, options: {
    types?: string[];
    fileName?: string;
    randomizeFileNameLength?: number;
    filePath?: string;
    fileMaxSize?: number;
} | null): Promise<unknown>;
export default base64toFile;
